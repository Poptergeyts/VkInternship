spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  main:
    allow-circular-references: true
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp

#pg_ctl start -D "C:\Data"